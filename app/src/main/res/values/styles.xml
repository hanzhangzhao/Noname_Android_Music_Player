<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/mainColor</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>
    
    <style name="line">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/brLineColor</item>
        <item name="android:layout_margin">@dimen/marginSize</item>
    </style>
    
    <style name="loginBtn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/btnHeight</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/titleSize</item>
        <item name="android:layout_marginLeft">@dimen/marginSize</item>
        <item name="android:layout_marginRight">@dimen/marginSize</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/btn_login_select</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation.Activity">

        <!--open an activity, new activity animation-->
        <item name="android:activityOpenEnterAnimation">@anim/open_enter</item>
        <!--open an activity, original activity animation-->
        <item name="android:activityOpenExitAnimation">@anim/open_exit</item>
        <!--when exit an activity, exit activity animation-->
        <item name="android:activityCloseExitAnimation">@anim/close_exit</item>
        <!--when exit an activity, back to screen activity animation -->
        <item name="android:activityCloseEnterAnimation">@anim/close_enter</item>

    </style>
</resources>
